# $('#mynote').html("<%= j render('edit_modal', note: @note) %>");
# $('#editModal').modal('show');

$('#(note_<%= note.id %>)').html("<%= j render 'edit_modal' , note: @note %>");
$('#editModal').modal('show');

# $("#note-<%= @note.id %>").html("<%= j render 'note', note: @note %>");
# $("#edit-modal").modal("hide");
